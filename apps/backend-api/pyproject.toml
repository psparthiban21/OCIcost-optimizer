[project]
name = "oci-cost-optimizer-backend"
version = "0.1.0"
description = "Python mock backend for the OCI Cost Optimizer dashboard."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
